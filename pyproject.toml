[project]
name = "sentryblur"
version = "0.1.0"
description = "Blur faces and license plates in dashcam footage"
requires-python = ">=3.11"
dependencies = [
    "click>=8.1",
    "numpy",
    "opencv-python",
    "tqdm",
    "imageio-ffmpeg>=0.5",
    "insightface",
    "onnxruntime",
]

[project.optional-dependencies]
plates = [
    "open-image-models",
]
prompt = [
    "torch>=2.4",
    "transformers>=4.57",
    "accelerate",
    "pillow",
]

[project.scripts]
sentryblur = "sentryblur.cli:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["sentryblur"]

[dependency-groups]
test = [
    "pytest>=7.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"
markers = [
    "gpu: integration tests that require CUDA or MPS hardware",
]
